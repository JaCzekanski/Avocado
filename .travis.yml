language: cpp
matrix:
  include:
    - os: linux
      env: NAME=clang6
      sudo: required
      dist: trusty
      cache: ccache
      install: "./.travis/linux/pull-container.sh"
      script: "./.travis/linux/run-container.sh"

notifications:
  - email: false
